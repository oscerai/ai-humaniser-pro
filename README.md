# AI Humaniser Pro

Makes regulatory documents (ISO 14971, IEC 62304, EU MDR) sound less AI-written — without changing meaning or dropping protected terms.

## Quick start
```bash
from pipeline_v2 import run_pipeline_v2
result = run_pipeline_v2(open('document.txt').read())
```
